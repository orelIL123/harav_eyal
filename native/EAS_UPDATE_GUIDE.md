# 📱 מדריך EAS Update - iOS ו-Android

## ✅ כן! EAS Update יעבוד לשניהם!

עכשיו ששניכם (iOS ו-Android) אישרו בחנויות, **EAS Update יעבוד על שני הפלטפורמות יחד!**

---

## 🔍 מה צריך לבדוק?

### 1. ✅ runtimeVersion תואם
**ב-`app.json` שלך:**
```json
{
  "expo": {
    "runtimeVersion": "1.0.0"
  }
}
```

**חשוב מאוד:** 
- כל ה-builds בחנויות חייבים להיות עם אותו `runtimeVersion`
- אם תשנה את `runtimeVersion`, תצטרך **build חדש לשניהם!**

### 2. ✅ Channel תואם
**ב-`eas.json` שלך:**
```json
{
  "build": {
    "production": {
      "channel": "production"
    }
  }
}
```

שניכם (iOS ו-Android) משתמשים באותו `channel: "production"` ✅

### 3. ✅ Updates מופעל
הקוד ב-`App.js` כבר בודק ומעדכן אוטומטית (שורות 115-164)

---

## 🚀 איך לשלוח Update לשניהם?

### פקודה אחת = Update לשניהם! 🎯

```bash
cd native
eas update --branch production --message "תיאור העדכון"
```

זה ישלח את ה-update **לשני הפלטפורמות יחד!**

### איך זה עובד?

1. **עשית שינויים בקוד JavaScript/TypeScript?**
   - תוכן, מסכים, לוגיקה, וכו'

2. **שלח update:**
   ```bash
   eas update --branch production --message "הוספתי כפתור הקדשה"
   ```

3. **המשתמשים יקבלו את העדכון:**
   - iOS יקבל ✅
   - Android יקבל ✅
   - **בלי build חדש!** 🎉

---

## ⚠️ מתי צריך Build חדש?

### צריך Build חדש רק אם:

1. **שינית `runtimeVersion`** ב-`app.json`
   ```json
   "runtimeVersion": "1.0.0" → "1.1.0"  // צריך build חדש!
   ```

2. **הוספת Native Module חדש** (package שלא היה קודם)
   - למשל: הוספת מצלמה, GPS, וכו'

3. **שינית Native Config:**
   - Permissions חדשים
   - שינוי ב-`app.json` שחייב build (כמו bundle identifier)

4. **שינית גרסת Expo SDK**
   - אם מעדכנים את `expo` בגרסה

### לא צריך Build חדש אם:

✅ שינית JavaScript/TypeScript code  
✅ הוספת מסכים חדשים  
✅ שינית תוכן מ-Firebase  
✅ תיקנת באגים  
✅ שינית עיצוב  

---

## 📋 סיכום - מה קורה עכשיו?

### ✅ מה שכבר מוכן:

1. **iOS:** Build בחנות + Updates מופעל ✅
2. **Android:** Build בחנות + Updates מופעל ✅
3. **Channel משותף:** `production` ✅
4. **runtimeVersion משותף:** `1.0.0` ✅
5. **קוד Updates:** מוכן ב-`App.js` ✅

### 🎯 מה שצריך לעשות:

**רק לשלוח updates!**
```bash
cd native
eas update --branch production --message "תיאור"
```

---

## 🔄 Workflow מומלץ

### 1. עבודה יומית (עדכוני תוכן/קוד):
```bash
# עשה שינויים בקוד
# ...

# שלח update
eas update --branch production --message "עדכון יומי - הוספת כפתור הקדשה"
```

### 2. עדכון גרסה (צריך build):
```bash
# 1. שנה את runtimeVersion ב-app.json (אם צריך)
# 2. Build לשניהם
eas build --platform ios --profile production
eas build --platform android --profile production

# 3. Submit לחניות
eas submit --platform ios
eas submit --platform android

# 4. אחרי שה-submitted, אפשר לחזור לשלוח updates רגילים
eas update --branch production --message "עדכון אחרי build"
```

---

## 📊 בדיקה - האם Updates עובד?

### בדיקה מהירה ב-App:

בקוד שלך יש כבר logging ב-`App.js`:
- `Updates.isEnabled` - צריך להיות `true`
- `Updates.channel` - צריך להיות `production`
- `Updates.runtimeVersion` - צריך להיות `1.0.0`

### בדיקה ב-EAS Dashboard:

1. לך ל: https://expo.dev/accounts/orel895/projects/harav-eyal/updates
2. תראה את כל ה-updates שנשלחו
3. תראה כמה משתמשים קיבלו כל update

---

## 🎓 דוגמאות

### דוגמה 1: עדכון תוכן
```bash
# שינית את הטקסט במסך הבית
eas update --branch production --message "עדכון טקסט שותפות"
# ✅ שני הפלטפורמות מקבלות את העדכון תוך דקות!
```

### דוגמה 2: תיקון באג
```bash
# תיקנת באג ב-JavaScript
eas update --branch production --message "תיקון באג בתצוגת הקדשות"
# ✅ שני הפלטפורמות מקבלות את התיקון!
```

### דוגמה 3: תכונה חדשה
```bash
# הוספת מסך חדש
eas update --branch production --message "הוספת מסך הקדשות יומיות"
# ✅ שני הפלטפורמות מקבלות את התכונה החדשה!
```

---

## ⚡ טיפים

1. **תמיד תן הודעת update ברורה:**
   ```bash
   eas update --branch production --message "הוספת כפתור הקדשה יומית + כפתור וואטסאפ"
   ```

2. **בדוק את ה-updates בדשבורד:**
   - תראה כמה משתמשים קיבלו
   - תראה אם יש שגיאות

3. **אם צריך לחזור לגרסה קודמת:**
   - בדשבורד יש אפשרות ל-rollback

4. **תמיד בדוק שאתה ב-branch הנכון:**
   ```bash
   eas update --branch production  # ✅ נכון
   # לא preview או development!
   ```

---

## 🆘 בעיות נפוצות

### בעיה: Update לא מגיע
**פתרון:**
1. בדוק ש-`runtimeVersion` תואם
2. בדוק ש-`channel` הוא `production`
3. בדוק שה-build בחנות תומך ב-Updates

### בעיה: צריך build חדש אבל לא יודע מתי
**כלל אצבע:**
- **אם שינית רק JavaScript/TypeScript** → רק update
- **אם שינית native code/config** → צריך build

---

## 📞 סיכום

**כן! עכשיו EAS Update יעבוד לשניהם (iOS ו-Android)!**

**פשוט שלח:**
```bash
cd native
eas update --branch production --message "העדכון שלך"
```

**וזה יעבוד לשניהם יחד!** 🎉


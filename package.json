{
  "name": "harav-eyal-native",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "native": "cd native && npm start",
    "native:ios": "cd native && npm run ios",
    "native:android": "cd native && npm run android",
    "init:backend": "node scripts/initBackend.js"
  },
  "dependencies": {},
  "devDependencies": {
    "firebase-admin": "^13.6.0"
  }
}
